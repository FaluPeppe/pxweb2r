% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pxweb2r.R
\name{pxweb2_get_data}
\alias{pxweb2_get_data}
\title{Get data from a PxWeb API v2}
\usage{
pxweb2_get_data(
  table = NULL,
  query = NULL,
  lang = "sv",
  output_format = "json-stat2",
  base_url = "https://statistikdatabasen.scb.se/api/v2/tables/",
  on_all_values_invalid = "stop",
  allow_label_values = TRUE,
  strip_code_in_label = TRUE,
  latest_period_code = "9999",
  allow_api_wildcards = TRUE,
  harmonise_variable_names = NULL,
  deso_regso_versions = "latest",
  split_deso_regso_by_municipality = TRUE,
  include_aggregations = "none",
  auto_limit = 30L
)
}
\arguments{
\item{table}{Table id (e.g. \code{"TAB6104"}), a vector of table ids, or a
metadata object from \code{\link[=pxweb2_get_metadata]{pxweb2_get_metadata()}}.}

\item{query}{Named list where each element is a variable and the value is the
values to fetch. \code{NULL} fetches all values. Both codes and labels are
accepted when \code{allow_label_values = TRUE}.}

\item{lang}{Language for metadata and labels, \code{"sv"} or \code{"en"}.}

\item{output_format}{Output format from the API, normally \code{"json-stat2"}.}

\item{base_url}{Base URL of the PxWeb API v2 (the tables endpoint).}

\item{on_all_values_invalid}{What happens when every supplied value for a
variable is invalid: \code{"stop"}, \code{"*"} or \code{"null"}.}

\item{allow_label_values}{If \code{TRUE}, queries may contain labels instead of
codes.}

\item{strip_code_in_label}{Removes codes that also appear in the label column.}

\item{latest_period_code}{Code that in a query means "fetch the latest time
period". \code{NULL} disables the feature.}

\item{allow_api_wildcards}{If \code{TRUE}, \code{"*"} in queries is passed on as an API
wildcard instead of being matched against valid values.}

\item{harmonise_variable_names}{Named vector for renaming variables so that
several tables get the same variable name, e.g. \code{c(Region = "Kommun")}.}

\item{deso_regso_versions}{Handling of multiple DeSO/RegSO versions:
\code{"latest"}, \code{"sum"} or \code{NULL}.}

\item{split_deso_regso_by_municipality}{If \code{TRUE}, \code{kommun_kod} and \code{kommun}
are added as separate columns.}

\item{include_aggregations}{Controls fetching of aggregations, see
\code{\link[=pxweb2_get_values]{pxweb2_get_values()}}.}

\item{auto_limit}{Max number of code-list calls when
\code{include_aggregations = "auto"}.}
}
\value{
A \code{tibble} with the fetched data.
}
\description{
Fetches data from one or more PxWeb tables (by default Statistics Sweden's
statistical database) and returns a tidy \code{tibble}. Handles, among other
things, automatic chunking of large requests, label values in queries,
wildcards, "latest period", and splitting DeSO/RegSO by municipality.
}
