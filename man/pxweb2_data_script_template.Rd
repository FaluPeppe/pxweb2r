% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pxweb2r.R
\name{pxweb2_data_script_template}
\alias{pxweb2_data_script_template}
\title{Build a ready-made pxweb2_get_data() call as text}
\usage{
pxweb2_data_script_template(
  table_id,
  default_value = "*",
  overrides = list(),
  to_clipboard = TRUE,
  base_url = "https://statistikdatabasen.scb.se/api/v2/tables/"
)
}
\arguments{
\item{table_id}{Table id.}

\item{default_value}{Value used for variables without an override, normally
\code{"*"}.}

\item{overrides}{Named list of values per variable.}

\item{to_clipboard}{If \code{TRUE}, the text is copied to the clipboard. Requires
the \code{clipr} package and an available clipboard (on Linux \code{xclip}, \code{xsel} or
\code{wl-clipboard} plus an active display). If not available, the text is only
printed.}

\item{base_url}{Base URL of the PxWeb API v2.}
}
\value{
Called for its side effect (\code{cat()}). Returns \code{NULL} invisibly.
}
\description{
Generates a complete \code{pxweb2_get_data()} call for a table, as text. Printed
with \code{cat()} and, by default, copied to the clipboard.
}
